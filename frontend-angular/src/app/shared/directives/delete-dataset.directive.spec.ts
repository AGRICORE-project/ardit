import { DeleteDatasetDirective } from './delete-dataset.directive';

describe('DeleteDatasetDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteDatasetDirective();
    expect(directive).toBeTruthy();
  });
});
