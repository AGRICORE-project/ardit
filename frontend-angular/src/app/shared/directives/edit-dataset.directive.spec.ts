import { EditDatasetDirective } from './edit-dataset.directive';

describe('EditDatasetDirective', () => {
  it('should create an instance', () => {
    const directive = new EditDatasetDirective();
    expect(directive).toBeTruthy();
  });
});
