import { PurgeCommentDirective } from './purge-comment.directive';

describe('PurgeCommentDirective', () => {
  it('should create an instance', () => {
    const directive = new PurgeCommentDirective();
    expect(directive).toBeTruthy();
  });
});
