import { UpdateCommentDirective } from './update-comment.directive';

describe('UpdateCommentDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateCommentDirective();
    expect(directive).toBeTruthy();
  });
});
